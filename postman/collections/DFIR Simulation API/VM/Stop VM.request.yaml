$kind: http-request
name: Stop VM
method: POST
url: '{{base_url}}/api/vm/stop'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "attemptId": "{{attempt_id}}"
    }
