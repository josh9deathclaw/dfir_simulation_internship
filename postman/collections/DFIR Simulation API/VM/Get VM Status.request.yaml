$kind: http-request
name: Get VM Status
method: GET
url: '{{base_url}}/api/vm/status/:attemptId'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
pathVariables:
  - key: attemptId
    value: '{{attempt_id}}'
