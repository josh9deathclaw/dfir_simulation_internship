$kind: http-request
name: Start VM
method: POST
url: '{{base_url}}/api/vm/start'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "attemptId": "{{attempt_id}}"
    }
