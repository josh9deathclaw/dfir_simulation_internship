$kind: http-request
name: Get Attempt for Grading
method: GET
url: '{{base_url}}/api/grading/attempts/:attemptId'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
pathVariables:
  - key: attemptId
    value: '{{attempt_id}}'
