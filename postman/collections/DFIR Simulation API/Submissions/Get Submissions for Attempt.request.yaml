$kind: http-request
name: Get Submissions for Attempt
method: GET
url: '{{base_url}}/api/submissions/attempt/:attemptId'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
pathVariables:
  - key: attemptId
    value: '{{attempt_id}}'
